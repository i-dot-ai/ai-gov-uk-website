---
layout: knowledge-hub/base.njk
templateEngineOverride: njk
pagination:
    data: knowledgeHubPrompts
    size: 1
    alias: prompt
permalink: "knowledge-hub/prompts/{{ prompt.title | slugify }}/"
---

<main class="govuk-width-container" data-pagefind-body>

  <div id="main" class="govuk-main-wrapper govuk-!-padding-bottom-9 govuk-!-padding-top-7">

    <dynamic-breadcrumbs category="{{ prompt.category }}" type="prompt"></dynamic-breadcrumbs>

    <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds">

        <h1 class="govuk-heading-xl govuk-!-margin-bottom-5">{{ prompt.title }}</h1>
        <p class="govuk-body-l">{{ prompt.description }}</p>

        <div class="govuk-!-margin-bottom-5">
            <span class="govuk-!-display-flex govuk-!-display-flex--align-center" style="flex-wrap: wrap; gap: 10px;">
                {% if prompt.verified %}
                    <strong class="govuk-tag govuk-tag--green govuk-!-margin-left-0">Verified</strong>
                {% else %}
                    <strong class="govuk-tag govuk-tag--pink govuk-!-margin-left-0">Unverified</strong>
                {% endif %}
            </span>
        </div>

        {# For search results #}
        <img src="/img/project-logos/logo-knowledge-hub.png" alt="Knowledge Hub" style="display: none;"/>
        {% if prompt.content %}
          <div id="prompt-content">
            {{ prompt.content | markdownToHtml(true) | safe }}
          </div>
        {% else %}
          {% if prompt.use %}
            <h2 class="govuk-heading-m">What this prompt does</h2>
            <div class="govuk-body">{{ prompt.use | markdownToHtml | safe }}</div>
          {% endif %}
          {% if prompt.beforeYouStart %}
            <h2 class="govuk-heading-m">Before you start</h2>
            <div class="govuk-body">{{ prompt.beforeYouStart | markdownToHtml | safe }}</div>
          {% endif %}
        {% endif %}
         {% if prompt.feedback %}
          <div class="govuk-notification-banner govuk-!-margin-bottom-5" role="region" aria-labelledby="govuk-notification-banner-title" data-module="govuk-notification-banner">
            <div class="govuk-notification-banner__header">
              <h2 class="govuk-notification-banner__title" id="govuk-notification-banner-title">Important</h2>
            </div>
            <div class="govuk-notification-banner__content">
              <p class="govuk-notification-banner__heading">Some people have flagged potential issues with this prompt. Use with caution - some users have shared
                  <a class="govuk-link" href="#feedback-details">feedback about potential issues.</a>
              </p>
            </div>
          </div>
        {% endif %}
        <h2 class="govuk-heading-m">The prompt</h2>
        <div class="prompt-text govuk-!-padding-3 govuk-!-margin-bottom-3" id="prompt-0">
          <pre class="govuk-!-margin-0" style="white-space: pre-wrap;">
            <code>
            {{ prompt.prompt }}
            </code>
          </pre>
        </div>
        <copy-button copy="prompt-0">
          Copy prompt
          <span class="govuk-visually-hidden">{{ prompt.title }}</span>
        </copy-button>
        {% if prompt.feedback %}
          <h2 class="govuk-heading-m govuk-!-margin-top-7" id="feedback-details">Feedback about this prompt</h2>
          <div class="govuk-body">{{ prompt.feedback | markdownToHtml | safe }}</div>
        {% endif %}
        {% if prompt.created %}
          <p class="govuk-body-s govuk-!-margin-bottom-0 govuk-!-margin-top-9">
            {% set created = prompt.created | dateFormat('int') %}
            {% set updated = prompt.updated | dateFormat('int') %}
            Content created: {{ created }}
            {% if updated != created %}
              | Last updated: {{ updated }}
            {% endif %}
          </p>
        {% endif %}
      </div>

      <div class="govuk-grid-column-one-third kh-sticky-sidebar">
          <h2 class="govuk-heading-m">Resources</h2>
          <p class="govuk-body">Here are the relevant guidance pages for using prompts:</p>

          <ul class="govuk-list">
              <li class="kh-light-box govuk-!-padding-3">
                  <a class="govuk-heading-s govuk-link" href="/knowledge-hub/how-to/how-to-use-ai-at-work">
                      How to use AI at work
                  </a>
                  <p class="govuk-body">What AI can do, when to use it and how to use it responsibly</p>
              </li>
              <li class="kh-light-box govuk-!-padding-3">
                  <a class="govuk-heading-s govuk-link" href="/knowledge-hub/how-to/experiment-with-prompts">
                      Experiment with prompts
                  </a>
                  <p class="govuk-body">Best-practise to create simple and complex prompts.</p>
              </li>
          </ul>
    </div>

    </div>

  </div>
</main>

<script type="module" src="/js/lit-components/dynamic-breadcrumbs.mjs"></script>
<script>
  window.addEventListener('load', () => {
    let copyButton = document.querySelector('.copy-button');
    copyButton?.classList.remove('copy-button');
  });
</script>
