---
title: Projects
layout: base.njk
templateEngineOverride: njk
noIndex: true
---

{% from "./_includes/banner.njk" import banner %}


{{ banner("Projects") }}

{% macro projectCardVertical(logo, title, url, synopsis, partner) %}
  <div class="iai-project-card-vertical govuk-grid-column-one-third-from-large-desktop govuk-!-display-flex govuk-!-padding-left-3 govuk-!-padding-right-3" style="align-items: flex-start;" data-aos="fade-up">
    <img class="iai-project-card-vertical__logo" src="{{ logo }}" alt="" loading="lazy"/>
    <div class="govuk-body govuk-!-margin-left-2">
      <a class="govuk-link govuk-!-font-weight-bold" href="{{ url }}">{{ title }}{% if partner %} ({{ partner }}){% endif %}</a>
      <p class="govuk-!-margin-top-2">{{ synopsis }}.</p>
    </div>
  </div>
{% endmacro %}

{% macro listProjectsByStage(stage) %}
    {% for project in projects %}
      {% if project['project-stage'] === stage %}
        {% if project.url %}
          {% set projectUrl = project.url %}
        {% else %}
          {% set projectUrl = project.title | slugify %}
        {% endif %}
        {% set logo = '/img/project-logos/logo-' + project.title | lower + '.png' %}
        {% if stage === 'Handed Over' %}
          {% set partner = project.partner %}
        {% else %}
          {% set partner = null %}
        {% endif %}
        {{ projectCardVertical(
          logo,
          project.title,
          projectUrl,
          project.synopsis,
          partner
        ) }}
      {% endif %}
    {% endfor %}
{% endmacro %}


<div class="govuk-width-container">
  <div class="govuk-main-wrapper">

    <h2 class="govuk-heading-m">Active</h2>
    <div class="govuk-grid-row govuk-!-margin-top-6 govuk-!-margin-bottom-6 govuk-!-padding-bottom-4 govuk-!-display-flex govuk-!-display-flex--wrap" style="gap: 30px 0; border-bottom: 1px solid #b1b4b6;">
      {{ projectCardVertical(
        '/img/project-logos/logo-knowledge-hub.png',
        'AI Knowledge Hub',
        '/knowledge-hub',
        'Read case studies and guidance for building AI technologies'
      ) }}
      {{ listProjectsByStage('Live Development') }}
    </div>

    <h2 class="govuk-heading-m">Partner-led</h2>
    <p class="govuk-body-s">Tools which have come through the Incubator and are now being delivered by partner organisations.</p>
    <div class="govuk-grid-row govuk-!-margin-top-6 govuk-!-margin-bottom-6 govuk-!-padding-bottom-4 govuk-!-display-flex govuk-!-display-flex--wrap" style="gap: 30px 0; border-bottom: 1px solid #b1b4b6;">
      {{ listProjectsByStage('Handed Over') }}
    </div>

    <h2 class="govuk-heading-m">Paused</h2>
    <div class="govuk-grid-row govuk-!-margin-top-6 govuk-!-margin-bottom-6 govuk-!-padding-bottom-4 govuk-!-display-flex govuk-!-display-flex--wrap" style="gap: 30px 0; border-bottom: 1px solid #b1b4b6;">
      {{ listProjectsByStage('Paused Development') }}
    </div>


  </div>
</div>
